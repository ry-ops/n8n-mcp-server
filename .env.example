# n8n MCP Server Environment Configuration

# n8n instance URL (required)
N8N_HOST=http://10.88.140.149:5678

# n8n API key (required)
# Generate at: n8n Settings -> n8n API -> Create API Key
N8N_API_KEY=your-api-key-here

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Optional: Request timeout in seconds
# N8N_TIMEOUT=30

# Optional: Verify SSL certificates (set to false for self-signed certs)
# N8N_VERIFY_SSL=true
